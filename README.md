# React + TypeScript + Vite

# *Main Structur*

- new structur

```bash
src/
  types/        → interfaces and types for the domain
  services/      → API communication layer (fetch/axios)
  hooks/          → useFetch, useAuth, etc.
  context/       → AuthContext
  components/  → reusable components
  pages/          → views
```